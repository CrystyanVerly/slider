export default class Slider {
  constructor(wrapper, track) {
    this.wrapper = document.querySelector(wrapper);
    this.track = document.querySelector(track);
  }

  init() {
    console.log('oi');
  }
}
